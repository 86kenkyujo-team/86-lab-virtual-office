# Optional: set these to use Supabase instead of the local JSON database.
# The secret/service role key is server-only. Do not expose it in browser code.
SUPABASE_URL=https://your-project-ref.supabase.co
SUPABASE_SECRET_KEY=your-secret-key
SUPABASE_SERVICE_ROLE_KEY=your-service-role-key
