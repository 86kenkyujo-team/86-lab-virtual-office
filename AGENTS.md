# 86研究所 仮想オフィス 作業ルール

## プロジェクト方針

- 本プロジェクトは、QR入室とリモート入室を分けて扱うバーチャル在籍管理MVPです。
- UIは `DESIGN.md` を優先し、ドット絵の仮想オフィスと実務向けの管理画面を両立します。
- 表示ブランド名は「86研究所」を正とします。

## 実装方針

- MVPでは外部依存を増やさず、Node.js標準機能のローカルJSON DBで動作確認できる状態を維持します。
- 将来のSupabase移行を見据え、データ項目名は `supabase/schema.sql` と揃えます。
- メンバーや在籍状態の初期値は `data/presence-db.json` を正とします。
- 画像素材は `public/assets` 配下に置き、UI側では古いブランド名が写り込まないように実装側のテキストで制御します。

## 確認観点

- PC幅と390px前後のスマホ幅で、テキストやボタンが重ならないこと。
- 「オフィス」と「リモート」のステータスが視覚的に分かれること。
- QR経由の入室とリモート入室が履歴に別の `entry_method` として残ること。
